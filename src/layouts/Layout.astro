---
import '../styles/global.css'
import Footer from '../components/Footer.astro'

interface Props {
	title?: string;
	description?: string;
	image?: string;
	canonicalOverride?: string;
	noindex?: boolean;
}

const { 
	title = 'AlexaVolt | Instalații Electrice Profesionale', 
	description = 'AlexaVolt — electrician autorizat ANRE Iași. Instalații electrice, tablouri Schneider și sisteme fotovoltaice pentru casa ta.',
	image,
	canonicalOverride,
	noindex = false
} = Astro.props;

const canonicalPath = Astro.url.pathname.endsWith('/') ? Astro.url.pathname : Astro.url.pathname + '/';
const canonicalURL = canonicalOverride ? new URL(canonicalOverride) : new URL(canonicalPath, Astro.site);
const ogImage = image || `${Astro.site}og-default.svg`;
---

<!doctype html>
<html lang="ro">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content={description} />
		{noindex && <meta name="robots" content="noindex, follow" />}
		<meta property="og:type" content="website" />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:url" content={canonicalURL.href} />
		<meta property="og:site_name" content="AlexaVolt" />
		<meta property="og:locale" content="ro_RO" />
		<meta property="og:image" content={ogImage} />
		<meta property="og:image:width" content="1200" />
		<meta property="og:image:height" content="630" />
		<meta name="twitter:card" content="summary" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={ogImage} />
		<link rel="canonical" href={canonicalURL.href} />

		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<script is:inline>
			window.dataLayer = window.dataLayer || [];
			function gtag(){dataLayer.push(arguments);}
			gtag('js', new Date());
			gtag('config', 'G-GGBL0JG2KZ');

			function loadGA() {
				if (document.getElementById('ga-script')) return;
				var s = document.createElement('script');
				s.id = 'ga-script';
				s.src = 'https://www.googletagmanager.com/gtag/js?id=G-GGBL0JG2KZ';
				document.head.appendChild(s);
			}

			// Load after user interaction or 3 seconds
			var events = ['scroll', 'click', 'touchstart', 'keydown'];
			var loaded = false;
			function onInteraction() {
				if (loaded) return;
				loaded = true;
				loadGA();
				events.forEach(function(e) { document.removeEventListener(e, onInteraction); });
			}
			events.forEach(function(e) { document.addEventListener(e, onInteraction, {once: true, passive: true}); });
			setTimeout(function() { if (!loaded) { loaded = true; loadGA(); } }, 3000);
		</script>
	</head>
	<body class="pb-16 md:pb-0">
		<a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-[200] focus:bg-blue-600 focus:text-white focus:px-4 focus:py-2 focus:rounded-lg">Salt la conținut</a>
		<slot />
		<Footer />
		<!-- Cookie consent banner -->
		<div id="cookie-banner" class="fixed bottom-0 left-0 right-0 z-[100] bg-slate-900 text-white px-6 py-4 shadow-2xl transform translate-y-full transition-transform duration-500" style="display:none;">
			<div class="max-w-4xl mx-auto flex flex-col sm:flex-row items-center gap-4">
				<p class="text-sm text-slate-300 flex-1">Acest site folosește cookies pentru analiză (Google Analytics). Continuarea navigării implică acordul tău.</p>
				<div class="flex gap-3">
					<button id="cookie-accept" class="bg-blue-600 text-white px-5 py-2 rounded-lg text-sm font-bold hover:bg-blue-700 transition-colors">Accept</button>
					<button id="cookie-reject" class="bg-slate-700 text-slate-300 px-5 py-2 rounded-lg text-sm font-medium hover:bg-slate-600 transition-colors">Refuz</button>
				</div>
			</div>
		</div>
		<script is:inline>
			(function() {
				var consent = localStorage.getItem('cookie-consent');
				if (consent === null) {
					var banner = document.getElementById('cookie-banner');
					if (banner) {
						banner.style.display = '';
						setTimeout(function() { banner.classList.remove('translate-y-full'); }, 100);
					}
				}
				document.getElementById('cookie-accept')?.addEventListener('click', function() {
					localStorage.setItem('cookie-consent', 'accepted');
					document.getElementById('cookie-banner')?.classList.add('translate-y-full');
					setTimeout(function() { document.getElementById('cookie-banner')!.style.display = 'none'; }, 500);
				});
				document.getElementById('cookie-reject')?.addEventListener('click', function() {
					localStorage.setItem('cookie-consent', 'rejected');
					document.getElementById('cookie-banner')?.classList.add('translate-y-full');
					setTimeout(function() { document.getElementById('cookie-banner')!.style.display = 'none'; }, 500);
					// Disable GA
					window['ga-disable-G-GGBL0JG2KZ'] = true;
				});
			})();
		</script>
		<!-- Bară fixă mobil: Sună + WhatsApp (doar pe mobil) -->
		<div class="fixed bottom-0 left-0 right-0 z-50 flex md:hidden shadow-2xl">
			<a href="tel:+40745539432" class="flex-1 flex items-center justify-center gap-2 bg-urgent-600 text-white font-bold py-4 active:bg-urgent-700 transition-colors">
				📞 Sună acum
			</a>
			<a href="https://wa.me/40745539432" target="_blank" rel="noopener noreferrer" class="flex-1 flex items-center justify-center gap-2 bg-wa text-white font-bold py-4 active:bg-wa-dark transition-colors">
				💬 WhatsApp
			</a>
		</div>

		<!-- WhatsApp floating button (desktop) -->
		<a 
			href="https://wa.me/40745539432" 
			target="_blank" 
			rel="noopener noreferrer"
			aria-label="Contactează-ne pe WhatsApp"
			class="hidden md:flex fixed bottom-6 right-6 z-50 w-14 h-14 bg-wa hover:bg-wa-dark rounded-full items-center justify-center shadow-lg shadow-emerald-200 hover:scale-110 transition-all duration-300"
		>
			<svg class="w-7 h-7 text-white" fill="currentColor" viewBox="0 0 24 24">
				<path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
			</svg>
		</a>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
</style>
