---
import { Image } from 'astro:assets';
import logoImg from '../assets/images/logo12.svg';
import { headerData } from '../data/navigation';

const { pathname } = Astro.url;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<!-- Bară de urgență (persistentă, toate paginile) -->
<div class="bg-urgent-600 text-white text-sm">
  <div class="max-w-7xl mx-auto px-4 md:px-6 flex items-center justify-center md:justify-between gap-3 py-2">
    <p class="hidden sm:flex items-center gap-2 font-semibold">
      <span class="inline-block w-2 h-2 rounded-full bg-white animate-pulse"></span>
      ⚡ Urgențe electrice în Iași — intervenție rapidă
    </p>
    <div class="flex items-center gap-3">
      <a href="tel:+40745539432" class="flex items-center gap-2 font-black tracking-tight hover:underline">
        📞 0745 539 432
      </a>
      <a href="tel:+40745539432" class="hidden md:inline-flex items-center bg-white text-urgent-700 font-bold px-4 py-1.5 rounded-full hover:bg-urgent-50 transition-colors">
        Sună acum
      </a>
    </div>
  </div>
</div>

<header class="bg-white border-b border-slate-100 sticky top-0 z-50 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 md:px-6 flex items-center justify-between py-4 md:py-6">
    
    <a href={import.meta.env.BASE_URL} class="flex-shrink-0 transition-transform active:scale-95">
      <Image 
        src={logoImg} 
        alt="AlexaVolt Logo" 
        class="h-14 md:h-24 w-auto"
        inferSize={true}
      />
    </a>

    <!-- Hamburger button (mobile only) -->
    <button 
      id="menu-toggle" 
      class="md:hidden flex flex-col items-center justify-center w-10 h-10 rounded-lg bg-slate-100 hover:bg-slate-200 transition-colors"
      aria-label="Deschide meniul"
      aria-expanded="false"
    >
      <span class="hamburger-line block w-5 h-0.5 bg-slate-700 transition-all duration-300"></span>
      <span class="hamburger-line block w-5 h-0.5 bg-slate-700 transition-all duration-300 mt-1.5"></span>
      <span class="hamburger-line block w-5 h-0.5 bg-slate-700 transition-all duration-300 mt-1.5"></span>
    </button>

    <!-- Desktop nav (hidden on mobile) -->
    <nav class="hidden md:block">
      <ul class="flex items-center gap-2 lg:gap-3">
        {headerData.links.map((link) => {
          const normalizedPath = pathname.replace(/\/$/, "");
          const normalizedHref = link.href.replace(/\/$/, "");
          const isActive = normalizedPath === normalizedHref || 
                           (link.href === import.meta.env.BASE_URL && (normalizedPath === "" || normalizedPath === base));
          
          return (
            <li class="whitespace-nowrap">
              <a 
                href={link.href} 
                class:list={[
                  "transition-all duration-300 px-3 py-2 lg:px-4 lg:py-2.5 rounded-xl text-sm lg:text-base font-black uppercase tracking-tight flex items-center gap-1.5",
                  isActive 
                    ? "bg-blue-600 text-white shadow-lg shadow-blue-100 scale-105" 
                    : "text-slate-700 hover:bg-slate-100 hover:text-blue-600 border border-transparent"
                ]}
              >
                {link.text}
              </a>
            </li>
          );
        })}
      </ul>
    </nav>
  </div>
</header>

<!-- Mobile drawer overlay -->
<div id="mobile-menu-overlay" class="fixed inset-0 bg-black/50 z-[60] hidden opacity-0 transition-opacity duration-300 md:hidden"></div>

<!-- Mobile drawer -->
<nav id="mobile-menu" class="fixed top-0 left-0 h-full w-72 bg-white z-[70] shadow-2xl transform -translate-x-full transition-transform duration-300 ease-in-out md:hidden" aria-label="Meniu mobil">
  <div class="flex items-center justify-between p-4 border-b border-slate-100">
    <a href={import.meta.env.BASE_URL} class="flex-shrink-0">
      <Image 
        src={logoImg} 
        alt="AlexaVolt Logo" 
        class="h-12 w-auto"
        inferSize={true}
      />
    </a>
    <button 
      id="menu-close" 
      class="w-9 h-9 flex items-center justify-center rounded-lg bg-slate-100 hover:bg-slate-200 transition-colors"
      aria-label="Închide meniul"
    >
      <svg class="w-5 h-5 text-slate-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>
  </div>
  
  <ul class="p-4 space-y-2">
    {headerData.links.map((link) => {
      const normalizedPath = pathname.replace(/\/$/, "");
      const normalizedHref = link.href.replace(/\/$/, "");
      const isActive = normalizedPath === normalizedHref || 
                       (link.href === import.meta.env.BASE_URL && (normalizedPath === "" || normalizedPath === base));
      
      return (
        <li>
          <a 
            href={link.href} 
            class:list={[
              "block px-4 py-3 rounded-xl text-base font-bold uppercase tracking-tight transition-all duration-200",
              isActive 
                ? "bg-blue-600 text-white shadow-md" 
                : "text-slate-700 hover:bg-slate-50 hover:text-blue-600"
            ]}
          >
            {link.text}
          </a>
        </li>
      );
    })}
  </ul>

  <div class="absolute bottom-0 left-0 right-0 p-4 border-t border-slate-100">
    <a 
      href={`${base}/contact/`}
      class="block w-full text-center bg-blue-600 text-white px-6 py-3 rounded-xl font-bold hover:bg-blue-700 transition-colors shadow-lg"
    >
      📞 Contactează-ne
    </a>
  </div>
</nav>

<script>
  const toggle = document.getElementById('menu-toggle');
  const close = document.getElementById('menu-close');
  const overlay = document.getElementById('mobile-menu-overlay');
  const menu = document.getElementById('mobile-menu');

  function openMenu() {
    menu?.classList.remove('-translate-x-full');
    overlay?.classList.remove('hidden');
    requestAnimationFrame(() => {
      overlay?.classList.remove('opacity-0');
      overlay?.classList.add('opacity-100');
    });
    toggle?.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }

  function closeMenu() {
    menu?.classList.add('-translate-x-full');
    overlay?.classList.remove('opacity-100');
    overlay?.classList.add('opacity-0');
    setTimeout(() => overlay?.classList.add('hidden'), 300);
    toggle?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  toggle?.addEventListener('click', openMenu);
  close?.addEventListener('click', closeMenu);
  overlay?.addEventListener('click', closeMenu);
</script>
